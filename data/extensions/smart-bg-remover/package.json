{
  "name": "smart-bg-remover",
  "displayName": "Smart Background Remover",
  "description": "Automatically detects the background pattern (solid color, checkerboard, stripes, dither weaves, gradient or noisy background) and removes it from all selected timeline frames. Live canvas preview with Ctrl+Shift+B, reuses the ' - removed' layer and hides the original without dialogs.",
  "version": "0.7.0",
  "author": {
    "name": "haminadablopes"
  },
  "publisher": "haminadablopes",
  "license": "MIT",
  "categories": [
    "Scripts"
  ],
  "contributes": {
    "scripts": [
      {
        "path": "./main.lua"
      }
    ],
    "keys": [
      {
        "id": "smartbg-keys",
        "path": "./keys.aseprite-keys"
      }
    ]
  }
}
